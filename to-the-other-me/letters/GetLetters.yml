info:
  name: GetLetters
  type: http
  seq: 2

http:
  method: GET
  url: "{{URI}}/api/v1/letters"
  auth: inherit

runtime:
  variables:
    - name: URI
      value: http://localhost:8000

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
